import Builder from "./core/Builder";
import registryInstance from "./core/registry";

import type {ComponentConfig, PropConfig, DefaultsSet, Project, ProjectType, View, Platform, PropType, Metadata} from "./core/types";

export {
  Builder,
  registryInstance
}

export type {
  Platform,
  PropType,
  Metadata,
  ComponentConfig,
  PropConfig,
  DefaultsSet,
  Project,
  ProjectType,
  View
}